[toolchain]
# 1.85 is the first stable that supports edition 2024 (transitive deps like
# indexmap 2.14 require it). The workspace itself stays on edition 2021.
channel = "1.90"
components = ["rustfmt", "clippy"]
